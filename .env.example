# Database connection string
DATABASE_URL=postgresql+psycopg2://localhost/libris

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Feed configuration
VIBE_THRESHOLD=0.7
PAGE_SIZE=10

# Sentiment analyzer type (textblob, hybrid, literary)
SENTIMENT_ANALYZER=literary
